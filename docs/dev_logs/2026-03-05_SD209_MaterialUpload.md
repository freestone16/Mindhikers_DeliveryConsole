# 📋 开发日志: SD-209 Material Upload (Phase 3 Loop Closure)

> **日期**: 2026-03-05  
> **模块**: Director Phase 3 - 非AI素材上传闭环  
> **实施者**: 老杨 (OldYang)  
> **状态**: ✅ 已完成

---

## 🎯 任务目标

解决 Phase 2 中选择"互联网素材" (`internet-clip`) 或"用户录屏" (`user-capture`) 后，只能打勾确认但没有物理文件产生的问题。实现 Phase 3 闭环：上传后的文件能被自动识别为 `"自有物料"` 参与 XML 合成。

---

## 📝 变更清单

### 1. 新增: `server/upload_handler.ts`
- **功能**: 处理非AI素材的视频上传
- **端点**:
  - `POST /api/director/upload-material` - 上传视频文件
  - `GET /api/director/material-exists` - 检查素材是否已存在
- **存储路径**: `projects/${projectId}/04_Visuals/${chapterId}_rendered.mp4`
- **特性**:
  - 支持 MP4、WebM、MOV 格式
  - 文件大小限制 500MB
  - 同名文件自动覆盖
  - 临时文件自动清理

### 2. 修改: `server/index.ts`
- **变更**: 注册上传路由
- **位置**: Line 22 (import) + Line 150-151 (routes)

### 3. 修改: `src/components/director/ChapterCard.tsx`
- **变更**: 为 `internet-clip` 和 `user-capture` 类型添加上传UI
- **新增功能**:
  - 选中条目后显示"上传视频"按钮
  - 上传状态实时反馈 (上传中/已完成)
  - 支持重新上传
  - 页面加载时自动检查已上传状态

---

## 🔧 实现细节

### 后端上传处理
```typescript
// 存储配置
- 目标路径: projects/${projectId}/04_Visuals/${chapterId}_rendered.mp4
- 临时目录: uploads/
- 命名规则: ${Date.now()}-${originalName}
```

### 前端UI状态机
```
未选中 -> 显示 "确认后上传"
已选中 + 未上传 -> 显示 "上传视频" 按钮
已选中 + 上传中 -> 显示 Loading 动画
已选中 + 已上传 -> 显示 "已上传" 状态 + 重新上传选项
```

---

## ✅ 验证计划

### 手动验证步骤
1. [ ] 在 Phase 2 选择一个"互联网素材"条目 (例如 1-1)
2. [ ] 点击绿勾确认该条目
3. [ ] 观察预览区域出现"上传视频"按钮
4. [ ] 点击按钮选择本地 MP4 文件
5. [ ] 验证 `04_Visuals` 目录生成 `ch1_rendered.mp4`
6. [ ] 进入 Phase 3，确认该条目被识别为"自有物料"
7. [ ] 验证 XML 合成包含该素材

---

## 🔗 关联文档

- 实施计划: `Implementation Plan-0305`
- 架构设计: `docs/02_design/sd209_expert_action_engine.md`
- 项目状态: `.agent/PROJECT_STATUS.md`

---

## 🚀 下一步

1. 老卢本地环境修复后，执行上述验证步骤
2. 如需支持更多视频格式，修改 `upload_handler.ts` 中的 `allowedTypes`
3. 考虑添加缩略图预览功能（上传后生成首帧预览图）

---

*Generated by OldYang Dev Logger @ 2026-03-05*
