# 2026-03-23 开发日志

## 会话主题：Phase 3 Bug 修复 + 状态持久化 + SkillSync 升级

---

### Commit 1: `9e29c41` fix(director): state persistence + duplicate confirm + Phase3 render stuck

**修复清单**：
1. **状态持久化（根因修复）**
   - `useExpertState.ts` 全面重写：新增 `projectId` 参数 + HTTP GET 优先恢复
   - `hydratedRef` 写保护机制防止空初始值覆盖后端数据
   - `App.tsx` localStorage 记住上次项目选择
   - `server/index.ts` 新增 `GET /api/expert-state/:expertId` 端点

2. **重复确认卡片**
   - `DirectorAdapter` 补充 `tryFastPath` → `director-bridge.ts` 快速路径
   - LLM 流式路径 `confirmAlreadyEmitted` 单次守卫

3. **Phase 3 "渲染中..." 卡住**
   - `pendingTaskKeys` Set 只增不减 → `onTaskComplete` 回调链

4. **预览图 Lightbox 溢出**
   - `max-width: 90vw; max-height: 85vh` + `object-fit: contain`

5. **环境路径统一**
   - `.env` PROJECTS_BASE/SKILLS_BASE 更新到新路径

### Commit 2: `cacca6c` feat(skill-sync): add RemotionStudio to SkillSync + StatusFooter dropdown

1. **RemotionStudio 加入 SkillSync**
   - 完整 Node 项目不走 cpSync，通过可达性检查以同等身份加入（6/6）

2. **StatusFooter 下拉面板**
   - 参考 GoldenCrucible 实现，深色主题适配
   - 点击展开/外部点击收起

3. **director.ts 路径解析统一**
   - 从 2 级升级为 3 级候选链（与 remotion-api-renderer / skill-sync 同源）

### 审计：Director ↔ RemotionStudio 集成
- ✅ 6 核心模板注册完整
- ✅ 渲染管线字段映射正确
- ✅ 04_Visuals/ 路径引用一致
- ⚠️ LLM props 无 schema 校验（低风险）
- ⚠️ SceneComposer fallback 静默（低风险）

---

### 会话 2：沙漏架构拉通 + 火山引擎修复 + Phase 3 信息图 BUG

#### 1. 沙漏架构拉通：模板 Catalog 权责回归 RemotionStudio
- **新建** `~/.gemini/antigravity/skills/RemotionStudio/catalog.md` — 19 个模板全覆盖（原 Director 手抄本仅 8 个）
- **改写** `server/skill-loader.ts`：
  - `loadDirectorResource()` → 泛化为 `loadSkillResource()` + 独立 `loadRemotionCatalog()`
  - `{{REMOTION_CATALOG}}` 占位符改为从 RemotionStudio 目录读取
- **删除** `Director/resources/remotion_catalog.md`（全局 + 本地两份副本）
- 新增 11 个模板可用：BentoBoxSaaS, KineticTypography, MetricRings, BarChartRace, MindmapFlow, RadarChart, VennDiagram, QuoteCard, HierarchyPyramid

#### 2. 火山引擎修复
- `.env`：图片接入点 `doubao-seedream-4-0-250828` → `ep-20260226162231-5tbxl`（后台 Seedream-5.0-lite）
- `server/volcengine.ts`：
  - 删除硬编码 fallback `ep-20260226142759-xqc82`，缺配置直接报错
  - 视频 API `resolution`/`duration` 从拼进 prompt 改为正确的 body 顶层参数
- 已验证：图片 API 返回 200，Seedream 5.0 成功生成

#### 3. Phase 3 信息图渲染 BUG 修复（根因）
- `server/director.ts:1765`：渲染分类 `['seedance', 'generative']` → `['seedance', 'generative', 'infographic']`
  - 根因：infographic 遗漏导致走 static 路径，复用 Phase 2 缩略图 PNG 而非生成视频
- `server/director.ts:1705`：Volcengine 轮询超时 120s → 300s（rules #87）

---

### 待续问题
- Phase 3 渲染待用户实际触发验证
- Remotion 3-1 / 3-4 描述与渲染不一致（可能缓存，待重新渲染验证）
- 11 个新模板待实际生成测试
