# 2026-03-26 开发日志

## SaaS 账号与承载量方案落盘

- 完成 `GoldenCrucible-SSE` 多账号方案联网调研与实施前文档：
  - `docs/plans/2026-03-26_GoldenCrucible_SSE_Multi_Account_Research_and_Plan.md`
- 按“安全 / 健壮 / 简单 / 自主可控 / Hobby 快速上线”重新收束推荐路线：
  - `Better Auth + Postgres + 自建 workspace kernel + 固定角色 RBAC + 单 web service`
- 补写本轮业务量估算与 Railway 档位讨论文档：
  - `docs/plans/2026-03-26_GoldenCrucible_Railway_Capacity_And_Business_Estimate.md`

## Linear 收口

- 已整理 `MIN-94 / MIN-38 / MIN-8` 下属 issue 完成度
- 已直接做一轮结构收口：
  - `MIN-94` 下旧周末上线拆卡 `MIN-95 ~ MIN-99` 收为 `Duplicate`
  - `MIN-38` 下已被现状吸收的子卡收为 `Done`
  - `MIN-38` 下旧方法插件卡收为 `Duplicate`

## 当前统一口径

- 下个窗口继续按：
  - `docs/plans/2026-03-26_GoldenCrucible_SSE_Multi_Account_Research_and_Plan.md`
  推进 SaaS 开发
- 首批 SaaS 仍只收在：
  - `Crucible 文本主链 + auth/session/workspace`
- 不把 Director / Shorts / Remotion 重媒体任务一起带上云

## 多账号 Phase 0/1 首刀实现

- 已在 SSE 仓库内接入首批多账号骨架：
  - `server/auth/index.ts`
  - `server/auth/account-router.ts`
  - `server/auth/workspace-store.ts`
  - `scripts/run-auth-migrations.ts`
  - `src/auth/*`
- 当前实现口径：
  - `Better Auth + Postgres`
  - 登录方式首刀先走 `email/password`
  - 登录后自动初始化 personal workspace
  - Header 可显示当前用户与 workspace，并支持退出
- `crucible autosave` 已开始按 workspace 路径隔离：
  - auth 关闭时仍兼容旧路径
  - auth 开启后改写到 `runtime/crucible/workspaces/<workspaceId>/autosave.json`

## 验证与阻塞

- 前端新增 auth 层定向 lint 已通过
- server auth 模块已通过 `tsx` 真实加载校验
- `npm run build` 仍被仓库既有市场模块 / 旧类型债阻塞，不是本轮新引入

## 下一步建议

1. 先准备好 `DATABASE_URL` 与 `BETTER_AUTH_SECRET`
2. 执行 `npm run auth:migrate`
3. 再进入：
   - `Crucible turn/conversation/artifact` 的 workspace-aware 改造
   - invite/member roles
   - Railway 环境变量与部署验证

## Railway / 数据库 / 线上验证闭环

- 已将当前 worktree link 到：
  - `GoldenCrucible-SaaS-Staging / production / golden-crucible-saas`
- 已新增 Railway Postgres 服务
- 已给 `golden-crucible-saas` 写入：
  - `DATABASE_URL`
  - `BETTER_AUTH_SECRET`
  - `BETTER_AUTH_URL`
- 已用 Railway 公网 Postgres 连接成功执行：
  - `npm run auth:migrate`
- 已确认数据库中存在表：
  - `user`
  - `session`
  - `account`
  - `verification`
  - `workspace`
  - `workspace_member`
  - `workspace_invitation`
  - `active_workspace`
- 已补齐 Railway 部署所需文件：
  - `.railwayignore`
  - `railway.json`
  - `package.json` 中 `build:railway` / `start`
- 已成功重新部署 staging
- 线上核验结果：
  - `/health` 返回 `status: ok`
  - `/api/account/status` 返回：
    - `authEnabled: true`
    - `emailPasswordEnabled: true`
    - `usingDefaultSecret: false`

## 三类登录入口推进

- 已按新要求把登录入口收束为三类：
  - `Google 登录`
  - `微信扫码登录`
  - `邮箱注册 / 登录`
- 后端 Better Auth 配置层已新增 provider 开关判断：
  - `GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET`
  - `WECHAT_CLIENT_ID / WECHAT_CLIENT_SECRET`
- `GET /api/account/status` 已新增返回：
  - `googleEnabled`
  - `wechatEnabled`
  - `wechatMode: "qr"`
- 前端登录页已新增两个第三方入口按钮：
  - Google 登录
  - 微信扫码登录
- 当前口径：
  - Google 走标准 OAuth 登录
  - 微信走 `微信开放平台 -> 网站应用登录 -> 二维码授权`
- 已补微信准备清单文档：
  - `docs/plans/2026-03-26_GoldenCrucible_SSE_WeChat_Web_Login_Preparation_Checklist.md`
- 当前尚未在线上真正启用 Google / 微信 provider：
  - 原因是 Railway 还未写入对应 provider 密钥

## 云端白屏热修

- 用户反馈云端首页白屏后，已做一轮真实线上排障：
  - 浏览器快照确认 `body` 内只有空的 `#root`
  - Playwright 控制台抓到根因：
    - `Invalid base URL: /api/auth`
- 根因定位：
  - `src/auth/client.ts` 把 Better Auth 的 `baseURL` 配成了相对路径
  - 该错误会在前端启动阶段直接抛出，导致 React 未挂载
- 已修复：
  - `src/config/runtime.ts`
    - 新增 `buildAbsoluteApiUrl()`
  - `src/auth/client.ts`
    - 改为给 Better Auth 传入绝对 `baseURL`
- 修复后验证：
  - 定向 eslint 通过
  - `npm run build:railway` 通过
  - Railway 已重新部署 production
  - Playwright 线上复测已看到登录页正常渲染，不再白屏

## Google 登录接通

- 已收到 Google OAuth Web client，并写入 Railway production：
  - `GOOGLE_CLIENT_ID`
  - `GOOGLE_CLIENT_SECRET`
- 线上接口核验：
  - `/api/account/status` 已返回：
    - `googleEnabled: true`
    - `wechatEnabled: false`
    - `wechatMode: "qr"`
- 浏览器复测：
  - 首页 `使用 Google 登录` 按钮已从 disabled 变为可点击
  - 点击后已成功跳转到 Google 登录页
  - Google 授权页中的 `redirect_uri` 已命中：
    - `https://golden-crucible-saas-production.up.railway.app/api/auth/callback/google`
- 当前状态：
  - Google 登录已具备真实测试条件
  - 微信仍待用户准备开放平台网站应用资料
