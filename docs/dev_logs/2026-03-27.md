# 2026-03-27 开发日志

## Google 登录进入真实可测状态

- Railway production 已确认：
  - `googleEnabled: true`
  - `wechatEnabled: false`
  - `wechatMode: "qr"`
- 线上登录页已确认 `使用 Google 登录` 按钮可点击。
- 用户现在可以直接用自己的 Google 账号做首登测试。

## SaaS 用户头像菜单骨架

- 已将顶部原有“用户文字信息 + 单独退出按钮”改为头像按钮入口：
  - `src/components/UserAvatarMenu.tsx`
  - `src/components/Header.tsx`
- 菜单结构已先搭好，包含：
  - `下载`
  - `会员计划`
  - `关于我们`
  - `历史对话`
  - `检查更新`
  - `Help`
  - `MindHikers 官网`
  - `退出`
- 当前策略：
  - `退出` 已隐藏到头像菜单内
  - `MindHikers 官网` 可跳转
  - `检查更新` 与其余项先给占位 toast，后续再接真实功能

## Chat 用户头像替换

- 已开始把对话区用户侧从“你”字圆块切到真实用户头像 / 昵称首字：
  - `src/App.tsx`
  - `src/components/ChatPanel.tsx`
  - `src/components/crucible/soulRegistry.ts`
- 当前实现口径：
  - 顶部用户头像菜单使用 auth session 的头像
  - Chat 输入区用户圆块优先使用 auth 头像
  - Crucible header badge 的用户位也会优先使用 auth 头像

## 红色叹号观察

- 当前我在云端登录页未复现用户提到的“右下角红色叹号”。
- agent-browser 现场截图未看到该组件。
- 暂时判断：
  - 可能是用户浏览器本地插件 / 扩展导致
  - 或者只在登录后某个具体页面态出现
- 后续若用户补图，需要按截图继续定位。

## 验证

- `npm run build:railway`：通过
- Railway production 已重新部署：通过
- 线上登录页未登录态：通过

## 历史中心闭环增强

- Crucible 本地快照已改为按 workspace 拆分 key：
  - `src/components/crucible/storage.ts`
  - `src/components/ChatPanel.tsx`
  - `src/SaaSApp.tsx`
- 历史中心已补到一版更完整的管理态：
  - 会话详情预览
  - 标题重命名
  - 归档 / 恢复活动态
  - `markdown` 导出
- 后端已补：
  - `PATCH /api/crucible/conversations/:conversationId`
  - markdown artifact export
- 发布治理文档已落盘：
  - `docs/02_design/crucible/2026-03-27_GoldenCrucible_SSE_SAAS_Release_Governance.md`

## 本地验证补充

- `npm run typecheck:saas`：通过
- `npm run build`：通过
- agent-browser 已确认本地 `http://127.0.0.1:5182/` 页面壳可正常打开，无白屏
- 因本地 auth 未启用，头像菜单中的“历史对话”入口无法直接在本地 UI 中点开
- 因此补做了一轮本地 fixture API smoke：
  - 列表：通过
  - 详情：通过
  - 改标题：通过
  - markdown 导出：通过
  - 归档状态回写：通过
- Railway CLI 在当前执行环境中受外部握手限制，未完成在线项目映射复核；本轮治理文档沿用仓库既有确认口径
