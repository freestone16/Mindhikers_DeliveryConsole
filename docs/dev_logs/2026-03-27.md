# 2026-03-27

## Director Phase2 卡住排查

- 现象：`CSET-Seedance2` 在 Director Phase2 停留 5 分钟以上，前端一直显示“正在为你的剧本生成视觉方案”。
- 排查结论：
  - 服务进程仍在运行，问题不是后端整体宕机。
  - 本次 `director_state.json` 已推进到 Phase2，但 `selection_state.json` 和 `phase2_分段视觉执行方案_CSET-Seedance2.md` 都没有更新。
  - 说明任务卡在 `generateGlobalBRollPlan()` 之前或期间，尚未进入结果落盘阶段。
  - 当前全局模型配置为 `kimi / kimi-k2.5`。
- 修复：
  - 在 `server/llm.ts` 新增统一 `fetchWithTimeout()`。
  - 为所有 LLM provider 的 `fetch` 增加默认 `300s` 超时。
  - 让超时错误落入 Director Phase2 现有 `timeout` 分支，避免前端无限等待。
- 验证：
  - 跑了 `npm run build`，但被仓库存量 TypeScript 错误阻塞，未能用全量构建证明本次改动。

## 视觉模型配置页增强

- 目标：让 Director 的“视觉模型配置”页具备统一的视觉服务凭证入口，不再只围绕火山引擎。
- 已完成：
  - 顶部“核心凭证区”改为通用视觉服务选择器。
  - 新增 `Google Gemini Image` 入口，用于保存 `GOOGLE_API_KEY`。
  - 下方新增“已配置视觉服务”列表，能展示已配置的 `Google Gemini Image` 与 `Volcengine`。
  - 后端 `llm-config` 已支持 Google 凭证的保存、读取和连通性测试。
- 当前边界：
  - 这一步只完成“可配置、可保存、可看见”。
  - Director 实际图生链路仍然主要走火山，引入 NanoBanana Pro 的真实生成调用需后续补接。

## Director 视觉模型“配置即所得”设计切换

- 用户明确要求停止继续打补丁，先从根上做设计。
- 已输出正式方案：
  - `docs/plans/2026-03-27_director-visual-model-config-as-truth.md`
- 方案核心结论：
  - 根因不是某个下拉没刷新，而是缺少统一的 `Director Visual Runtime Router`
  - 必须把配置层、模型目录层、执行路由层、provider adapter 层拆开
  - Director 所有图生入口都应统一先读 `experts.director.imageModel / generation.imageModel`，再路由到对应 provider
