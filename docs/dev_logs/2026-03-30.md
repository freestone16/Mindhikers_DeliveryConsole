# 2026-03-30 开发日志

## MIN-94 收口复核

- 已复核 `MIN-94` 当前真实结构：
  - `MIN-95 ~ MIN-99`：均已收为 `Duplicate`
  - `MIN-104`：已为 `Done`
  - `MIN-105`：仍为 `In Progress`
  - `MIN-106`：仍为 `Todo`
- 结论：
  - `MIN-94` 下面旧周末上线拆卡没有遗漏尾项
  - 当前“该收的”已经收掉
  - 但 `MIN-94` 作为总协调卡暂时仍不能关闭，原因是 `MIN-105` 还未完成正式测试收口

## SAAS 当前测试交接

- 线上 `SAAS` 当前可以进入隔壁窗口做一轮初测
- 干净浏览器页访问 `https://golden-crucible-saas-production.up.railway.app/` 时，可停留在当前站点，页面首屏处于 `Checking account session...`
- 当前交接给隔壁的测试重点：
  - auth session
  - personal workspace
  - Crucible 历史中心
  - markdown 导出
  - 首页可达性与登录后基础链路

## 正式测试前的遗留工作

- `GoldenCrucible` 产品域名必须与 Homepage / CMS 隔离
- 当前拟定正式主域名：
  - `gc.mindhikers.com`
- 在正式测试前，仍需由 `SAAS` 侧完成并统一：
  - Railway 自定义域名绑定
  - `APP_BASE_URL`
  - `BETTER_AUTH_URL`
  - `CORS_ORIGIN`
  - Google / 微信 OAuth 回调域名

## 给隔壁 SAAS 测试的一段话

`MIN-94` 下面旧拆卡已收干净，当前真正活跃的只剩 `MIN-105` 和后续 `MIN-106`。SSE 这边已经把本轮应进生产的关键业务代码推到当前 `SAAS` 线上，Railway 临时域名可以先做初测；但 `MIN-94` 还不能整体关闭，因为正式测试前还要先把 GoldenCrucible 的独立产品域名收口到 `gc.mindhikers.com`，并统一 `APP_BASE_URL / BETTER_AUTH_URL / CORS_ORIGIN / OAuth 回调`。隔壁这轮请优先围绕 auth session、workspace、历史中心、markdown 导出和首页可达性做 SaaS 测试。`
