# 2026-04-01 开发日志

## 本轮主题

- 评估微信接入在 `SSE` 与 `SAAS` 两条线上的实施落点
- 形成可迁移的 `Authing` 最小接入方案
- 明确 `SAAS -> SSE` 反向同步治理口径

## 本轮关键判断

1. 本轮微信接入 feature 不建议先在 `SSE` 完成再 cherry-pick 到 `SAAS`
2. 更稳妥的实施落点应为：
   - `/Users/luzhoua/MHSDC/GoldenCrucible-SaaS`
3. 原因不是“`SAAS` 变成主研发现场”，而是：
   - 微信扫码登录强依赖正式域名、线上回调、环境变量与真人 smoke
   - 当前这些条件明显由 `SAAS` 承接得更成熟
4. 长期职责仍保持：
   - `SSE` = 共享业务逻辑演进源头
   - `SAAS` = 对外正式发布线 / staging / 线上收口

## 本轮方案输出

已新增正式实施方案：

- `docs/plans/2026-04-01_GoldenCrucible_SaaS_WeChat_Authing_And_Sync_Implementation_Plan.md`

该方案明确：

1. 继续保留 `Better Auth`
   - Google / 邮箱
2. `Authing` 当前只做微信扫码的上游认证
3. 业务真相层仍由我们自己的云端用户体系掌握
4. 短期继续使用本地 Postgres `"user"` 作为统一平台用户表
5. 新增 `user_identity` 承接多来源身份映射
6. 增加 `session facade`，避免业务 API 直接依赖供应商 session

## 本轮治理结论

建议正式建立 `SAAS -> SSE` 的定期反向同步机制，但必须遵守：

1. 只回灌共享底座
2. 不回灌发布线专属内容
3. 同步触发以：
   - 发布后回灌
   - 每周清账回灌
   为主

本轮方案中已单列要求：

- 下一窗口在 `GoldenCrucible-SaaS` 编码前，先把 4 条精炼规则补进：
  - `docs/04_progress/rules.md`

## 当前状态

- 本轮完成的是：
  - 调研
  - 架构判断
  - 正式实施方案落盘
  - 进度与交接文档更新
- 本轮尚未开始：
  - `GoldenCrucible-SaaS` 实际代码实现

## 下一窗口建议动作

1. 打开：
   - `/Users/luzhoua/MHSDC/GoldenCrucible-SaaS`
2. 先读：
   - `docs/dev_logs/HANDOFF.md`
   - `docs/04_progress/rules.md`
   - `/Users/luzhoua/MHSDC/GoldenCrucible-SSE/docs/plans/2026-04-01_GoldenCrucible_SaaS_WeChat_Authing_And_Sync_Implementation_Plan.md`
3. 在 `SAAS` 补 rules
4. 再进入微信/Authing 接入实施

