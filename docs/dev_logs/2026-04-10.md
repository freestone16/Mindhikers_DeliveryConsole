# Dev Log - 2026-04-10

**Session**: Unit 1 & 2 Implementation  
**Branch**: `feat/unit1-persona-profile`  
**Commits**: `5e021fa` (Unit 1), `983e86e` (Unit 2)

---

## 上午：Unit 1 工作内容

### 1. 方案落盘
- 创建 V2.1 方案文档，锁定 Unit 1 实施口径
- 明确文件路径、热插拔策略、实施顺序

### 2. Schema 开发
- 创建 `src/schemas/persona.ts`
- 定义 PersonaProfile Zod schema
- 包含：身份、认知定位、立场向量、发言规则、对比锚点、诚实边界

### 3. Loader 开发
- 创建 `server/persona-loader.ts`
- 实现 `loadAllPersonas()` 和 `loadPersonaBySlug()`
- 按需重读策略（无缓存、无 watch）

### 4. 7 哲人配置
- 创建 `personas/` 目录（仓根）
- 编写 7 个哲人 JSON 文件
- 全部通过 Zod 校验

### 5. 测试补充
- 创建 `server/__tests__/persona-loader.test.ts`
- 7 项测试全部通过
- 更新 vitest.config.ts 包含 server 目录

---

## 晚上：Unit 2 工作内容

### 1. V2.1 方案落盘
- 创建 `docs/plans/2026-04-10_unit2-proposition-sharpener-v2.1.md`
- 锁定模型：kimi-k2.5
- 锁定 API 契约：SharpenResult

### 2. LLM 分层路由
- 扩展 `server/llm.ts`
- 添加 `ModelTier` 类型：fast | standard | premium
- 添加 `callRoundtableLlm({ tier })` 函数
- 统一使用 kimi-k2.5（temperature=1）

### 3. 命题锐化模块
- 创建 `server/proposition-sharpener.ts`
- Zod schema: `SharpenResultSchema`
- Prompt 模板：4 个锋利标准
- 降级策略：LLM 失败返回 isSharp=true

### 4. API 端点
- `POST /api/roundtable/sharpen` —— 检测并锐化
- `POST /api/roundtable/sharpen/apply` —— 应用结果
- 输入校验 + fallback 处理

### 5. 测试
- 创建 `server/__tests__/proposition-sharpener.test.ts`
- 17 项测试覆盖：正常流程、detect 模式、fallback、边界情况

---

## 验证结果

```bash
# 类型检查
$ npm run typecheck:full
# 零错误 ✅

# 测试
$ npm run test:run
# 24 tests passed ✅ (Unit 1: 7, Unit 2: 17)

# Loader 验证
$ npx tsx -e "const { loadAllPersonas } = require('./server/persona-loader'); console.log(loadAllPersonas().map(p => p.slug))"
# ['charlie-munger', 'hannah-arendt', 'herbert-simon', 'nietzsche', 'richard-feynman', 'socrates', 'wang-yangming']
```

---

## 关键决策

| 决策 | 选择 | 理由 |
|------|------|------|
| persona 目录 | `personas/`（仓根） | 便于运营编辑和摘樱桃 |
| 热插拔策略 | 按需重读 | 简单可靠，后续可升级 |
| 7 哲人立场向量 | 四维差异化分布 | 确保 prompt 输出可区分 |
| **Unit 2 模型** | **kimi-k2.5** | 用户指定，速度快成本低 |
| **Temperature** | **1** | kimi-k2.5 限制，通过 prompt 补偿 |
| 降级策略 | 失败返回 isSharp=true | 不阻塞用户体验 |

---

## 下一步

**Unit 3**: 圆桌引擎核心
- Speaker Selection
- Moderator (开场 + 综合)
- Philosopher Loop (流式输出)

---

*按 OldYang 三层日志协议维护*
