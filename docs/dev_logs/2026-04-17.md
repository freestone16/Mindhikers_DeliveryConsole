# 2026-04-17

## 工作内容

- 阅读并提炼以下文档中的 UI / Shell 结构约束：
  - `GoldenCrucible-SSE/docs/plans/2026-04-17_UI_Architecture_Phase1-6_Implementation_Plan.md`
  - `GoldenCrucible-SSE/docs/plans/2026-04-16_UI_Architecture_PRD_v1.0.md`
- 回看 `Director` 当前界面代码与历史设计文档：
  - `src/App.tsx`
  - `src/components/Header.tsx`
  - `src/components/ExpertNav.tsx`
  - `src/components/DirectorSection.tsx`
  - `src/components/director/Phase1View.tsx`
  - `src/components/director/Phase2View.tsx`
  - `docs/02_design/delivery/_master.md`
  - `docs/archive/plans/2026-02-14-expert-navigation-design.md`
- 查看现有首页截图，确认当前真实结构仍是：
  - 顶部产品级切换
  - 第二层专家横向 tab
  - Director 内部 phase 按钮
  - Header 右侧项目 / 文稿上下文

## 产出

- 新增 `docs/plans/2026-04-17_director-ui-revamp-plan.md`
- 覆盖更新 `docs/dev_logs/HANDOFF.md`
- 新增 `src/components/DirectorUIDemoPage.tsx`
- 新增 `src/components/DirectorUIDemoPage.css`
- 在 `src/App.tsx` 挂载 `/#/director-ui-demo`
- 新增截图 `testing/director/artifacts/director-ui-demo-2026-04-17.png`

## 关键结论

1. 这轮不能只做 Director 页面换皮，必须先收口 `DeliveryConsole` 的共享 Shell
2. `Director` 应成为第一块新工作台样板，其他模块入口一起按同一协议预留
3. 推荐风格是“深色壳 + 暖色工作台”，而不是把 GoldenCrucible 的浅暖大底整页照搬过来
4. `VisualAudit` 应降到工具区，`LLMConfig` 应从 Header 孤立图标移到 Dock / 工具菜单
5. 若进入编码，实现顺序仍要服从 `PR0 Security Hotfix（MIN-122）` 前置

## demo 实作补充

- 按用户提供的 GoldenCrucible 截图，做了一版可打开的视觉 demo：
  - 左栏：`WORKSTATIONS / SESSIONS / Project Context Dock`
  - 中栏：`Director` 的概念稿纸面工作台
  - 右栏：`Artifact / Loaded Engines / Anchors / Citations / Handoff CTA`
- 后续按用户提醒继续收口为 **phase-aware demo**：
  - `P1 概念`
  - `P2 视觉方案`
  - `P3 渲染编排`
  - `P4 导出交付`
  - 四个阶段点击后，中区标题、facts、cards、原则区与右栏 drawer 语义都会切换
- 浏览器实际核验：
  - 启动 `vite preview` 于 `http://127.0.0.1:4173`
  - 用 `agent-browser` 打开 `/#/director-ui-demo` 并截图
  - 实际点过 `P2 / P3 / P4`，确认不是假标签
- 构建结果：
  - `./node_modules/.bin/vite build` 通过
  - `npm run build` 仍被仓库既有 TypeScript 历史错误阻塞

## live demo 基准补充

- 后续用户要求升级为：
  - 不再只是“参考截图”
  - 要尽可能向别人本地 `http://localhost:5273/` 的 demo **完全复刻**
  - 重点包括字体大小等细节
- 重新检查后确认：
  - `http://localhost:5273/` 已可访问
  - `curl -I http://localhost:5273/` 返回 `200 OK`
  - `agent-browser` 可正常打开
  - 页面标题为 `GoldenCrucible · UI North Star · 4-Screen Demo`
  - 右上角有 `01 / 02 / 03 / 04` 四屏切换
- 这意味着下一窗口不应再靠截图估风格，而应直接对 live demo 做逐屏视觉审计，再写 CE 团队详细实施方案

## 未做

- 未开始任何 UI 编码
- 未修改运行时代码
- 未跑测试

---

# 2026-04-17 下半场 · Remotion Broll 扩充方案规划（老杨）

## 输入

- 用户带来外部参考：`https://github.com/op7418/Video-Wrapper-Skills`
- 用户诉求：评估该仓库对导演大师 + RemotionStudio 的借鉴价值；是否丰富 Broll；是否与 UI 重构计划冲突；最终出一揽子方案

## 工作内容

- 分两路调研：
  - 第 1 路：WebFetch 读取 `op7418/Video-Wrapper-Skills` 仓库结构、Skill 定义、模板组件、红线规范
  - 第 2 路：Explore 子代理摸清 RemotionStudio 位置、现有 33 个模板清单、Director Phase2/3 集成链路、已有术语/数字类组件
- 得到 3 条颠覆性发现：
  1. Remotion 组件在外部 Skill 仓库 `~/.gemini/antigravity/skills/RemotionStudio/`，不在 Director 仓库里；catalog.md 是单一事实来源
  2. RemotionStudio 已有 NumberCounter / SegmentCounter / GaugeChart / StatDashboard / MetricRings 五个数字类组件，**AnimatedStats 是重复造轮子**，必须撤销
  3. **TermCard（术语定义卡）** 确实没有同类组件，值得新建
- 审视 Phase2→Phase3 链路，识别 5 处可优化缝隙：
  - props schema 无前置校验（等到 Phase3 才失败）
  - 缩略图失效机制不完整（rules.md #85 标注过，未落代码）
  - 配额规则只在 prompt 里，无运行时 guard
  - catalog 5 分钟缓存无法手动 bust
  - Phase3 失败降级缺失（本方案不做，等 PR0 解锁后单独立项）
- 评估与 UI 重构方案 `2026-04-17-001` 的冲突：
  - 物理文件冲突：仅 `Phase2View.tsx` 一处，冲突面极小
  - 协议层协调：新 SSE 事件类型需要 Drawer 运行态 tab 做兜底渲染
  - 外部共享阻塞：PR0 / MIN-122（同步而非冲突）

## 产出

| 文件 | 内容 |
|---|---|
| `docs/plans/2026-04-17-002-remotion-broll-enrichment-plan.md` | Broll 扩充与 Phase2-Phase3 协同优化方案（一揽子方案第 2 棒，UI 方案合并后再启动） |

方案核心结构：

- 8 条 Requirements Trace（R1-R8）
- 6 个 Implementation Units（Phase A→D 排期）
- 顶部「⚠️ 执行前置」章节 + 3 项启动前置检查清单
- 「本方案对 UI 方案落地物的依赖」对照表
- 串行执行说明（为什么 UI 先做）
- 覆盖 Risks 与 Mitigation 针对串行启动的三类风险

## 过程偏差与回滚

- 用户明确说"只要方案，不要实施，实施在外包团队"之前，本轮已越界动手做 TermCard 组件开发（改动 4 个文件 + 新建 TermCard.tsx）
- 收到纠正后立即全部回滚：
  - 用备份还原 4 个原始文件
  - 删除 TermCard.tsx
  - grep 验证零残留
- 反思：rules.md 应补一条"只写方案不实施"边界，避免同类越界

## 关键决定

1. 一揽子方案采用**串行执行**：UI 方案（第 1 棒）→ Broll 方案（第 2 棒）
2. 所有协调语义**只写进 Broll 方案**，UI 方案一字不动
3. 严格禁止"双改 Phase2View.tsx"——Broll Unit 4 必须等 UI Unit 4 完成再开工
4. 新增 SSE 事件必须有 Drawer 运行态 tab 的兜底渲染——属于 rules.md #21 的延伸实现
5. **AnimatedStats 撤销**，替代为 Broll prompt 层补齐 5 个数字组件的决策矩阵
6. TermCard 落到 RemotionStudio skill 仓库，不放 Director，严格遵守"单一事实来源"原则

## 未做（明确划边界）

- 未写任何一行实施代码
- 未修改 RemotionStudio 任何文件
- 未修改 Director 任何 server 或 src 文件
- 未跑 Phase2/3 验证
- 未等待 PR0 解锁（本方案不依赖）
- 未启动外包团队工作（等用户交接）

## 给下一会话/外包团队的交接要点

见 `docs/dev_logs/HANDOFF.md`——新会话第一个读那个文件即可完成上下文恢复。
