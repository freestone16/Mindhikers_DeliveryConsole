# 2026-04-21

## director-final 分支重建与收口

- 从 `origin/main` 新建干净分支 `director-final`
- 选择性迁移 Director 相关代码，避免把旧长寿分支里的 `HANDOFF`、测试截图、`__pycache__`、运行产物一并带入
- 将 Director 重新接回当前 SaaS shell，而不是直接回滚到旧版单体入口

## 运行时依赖补齐

- 修复 `server/director.ts` 启动缺失：
  - 新增 `server/svg-architect.ts`
  - 新增 `server/google-gemini-image.ts`
  - 新增 `src/schemas/visual-models.ts`
- 修复 `server/director-visual-runtime.ts` 与当前分支旧版 generation 配置结构之间的兼容问题
- 明确区分旧 worktree 服务与新 worktree 服务，避免误把旧目录 `3005` 进程当成 `director-final` 的后端

## 验证结果

- `npm run typecheck:full` 通过
- Director 相关测试通过：
  - `src/__tests__/director-bridge.test.ts`
  - `src/__tests__/director-adapter.test.ts`
  - `src/components/director/ChapterCard.test.tsx`
  - `src/components/director/Phase1View.test.tsx`
  - `src/components/director/Phase2View.test.tsx`
  - `src/components/director/Phase3View.test.tsx`
  - `src/components/director/Phase4View.test.tsx`
- 本地服务已跑通：
  - 前端 `http://localhost:5176/`
  - 后端健康检查 `http://localhost:3004/health`

## 遇到的问题

- `agent-browser` 在当前环境受本地 socket/daemon 权限限制，无法稳定回传页面快照
- Playwright 因运行目录权限问题无法正常初始化
- Safari 窗口验证只能作为辅助判断，后续仍需补一轮正式浏览器验收证据

## 当前阻塞

- `git commit` / `git push` 仍缺明确 Linear issue 编号
- 当前工作树还没按逻辑单元拆成最终 commit
