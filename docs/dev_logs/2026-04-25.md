# 2026-04-25

## 摘要

- 复核 `MHSDC-DC-director` 与 `DirectorFinal` 的路径治理对等性
- 结论：两边仍有大量差异；目标分支已具备 DirectorFinal 中确认安全的路径治理能力，本轮只摘取 `StatusFooter` 的 Director 身份文案小补丁
- 更新 `HANDOFF.md`，纠正远程状态并进入提交前分拣阶段

---

## 今日完成

### 1. 冻结当前现场

- 当前目录：`/Users/luzhoua/MHSDC/DeliveryConsole/Director`
- 当前分支：`MHSDC-DC-director`
- 远程状态：`origin/MHSDC-DC-director...HEAD` 为 `0 0`，无 ahead/behind
- 当前工作树仍有意保留未提交改动，等待验证复跑、分拣和 Linear issue 确认

### 2. 复跑路径治理扫描

执行结果：

- `server/project-root.ts` 不存在
- 非 backup 文件中 `project-root` 旧引用为 0
- 除 `server/project-paths.ts` 和 `src/__tests__/server/project-paths.test.ts` 外，没有业务代码直接读取 `process.env.PROJECTS_BASE`
- `server/index.ts` 只保留 `PROJECTS_BASE` 的说明性注释，不属于风险项

### 3. 对照 DirectorFinal 关键文件

本轮只对照路径治理相关能力，不做整枝 merge / cherry-pick。注意：这不是说两边没有差异，而是先判断 DirectorFinal 中哪些差异属于本轮可摘范围。

已确认目标分支具备：

- `server/project-paths.ts` 与 DirectorFinal 一致
- `ensureProjectsBaseExists()` 在服务启动前执行
- `/api/projects` 运行时读取 `getProjectsBase()`
- `/api/projects/switch` 使用 `getProjectRoot(projectName)`
- `server/distribution.ts` 队列文件路径运行时读取 `getProjectsBase()`
- `src/__tests__/server/project-paths.test.ts` 已存在
- Header / ProductTopBar 已有项目 loading / error / empty 三态
- `.env.example` 已声明 `PROJECTS_BASE=/path/to/your/Projects`

不回灌：

- SaaS/Auth/Crucible shell 差异
- GoldenCrucible / SaaS-only 路由、认证、历史会话
- 品牌文案变化
- `DeliveryShellLayout` 移除 `ProductTopBar` 的结构差异
- `delivery-shell.css` 布局高度策略

### 4. 本轮实际 cherry-pick

新增审计文档：

- `docs/plans/2026-04-25-directorfinal-cherrypick-diff-audit.md`

实际代码摘取：

- `src/components/StatusFooter.tsx`
  - `SYSTEM ONLINE` -> `DIRECTOR ONLINE`
  - `MindHikers Console` -> `Director Console`

实际治理摘取：

- `docs/04_progress/rules.md`
  - 增加“重建模块分支时必须同时迁移路径治理、`.env.example` 和启动口径”的事故规则

未摘：

- DirectorFinal 中的 `activeChatExpertId` / `/api/llm-config/chatbox` / runtime config 相关实现，因为目标分支当前没有对应接口与调用链，直接搬会扩大范围

### 5. 两窗口内收口判断

本轮已批量完成 DirectorFinal 差异判定，不再按“一颗一颗”无限摘。

后续只剩两类：

- 可提交分拣：已摘安全项与此前已有清理 diff
- 需老卢明确决策：是否另立题处理 SaaS/Auth/Crucible shell、ProductTopBar 移除、布局高度策略

## 当前结论

本轮没有发现 `MHSDC-DC-director` 缺少 DirectorFinal 的路径治理等价修复。DirectorFinal 仍有不少剩余差异，但它们主要属于 SaaS/Auth/Crucible shell、布局策略、历史会话或已在此前窗口回灌过的能力，不应在本轮重复搬运。

因此本轮只摘独立 footer 身份文案补丁与一条事故治理规则，不重复摘路径治理补丁；下一步按主题拆分当前已有未提交改动。

## 验证结果

- `npm run build`: 通过
- focused tests: 8 files / 46 tests 通过
- build 仅有既有 CSS minify warning 与 chunk size warning，不阻塞
- Agent Browser: 默认 delivery shell 主界面可打开，无页面错误；当前默认 Director shell 不渲染 `StatusFooter`，所以 footer 文案未在默认 DOM 中出现

## 下一步

1. 输出提交拆分建议
2. commit 前等待老卢确认 Linear issue 与提交拆分
