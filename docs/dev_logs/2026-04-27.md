# 2026-04-27 — SSOT 迁移到 Mindhikers/.claude/skills

## 目标

把导演大师涉及的全部 skill 从 `~/.gemini/antigravity/skills/` **复制**到 `/Users/luzhoua/Mindhikers/.claude/skills/`，并把 SSOT 切到新路径；旧路径保留不动。

## 决策（与老卢确认的）

- **Q1 范围**：C → 5 个 skill（Director、RemotionStudio、svg-architect、remotion-best-practices、remotion-visual-qa）
- **Q2 目标**：Claude Code 项目级 skill 约定路径 `Mindhikers/.claude/skills/`
- **Q3 旧 SSOT**：完整保留，不删不动；GoldenCrucibleLab 等其他消费方走自己的事
- **Q4 RemotionStudio 排除**：测试动画/历史产物（out/renders/payloads/test_*）不带，核心源码 + node_modules + catalog/SKILL/docs 必带
- **Q5 同名冲突**：`Mindhikers/.claude/skills/Director/` stub 重命名为 `Director.stub-bk-20260427/` 备份

## 执行步骤

### 1. 物理 copy（零删除）

```bash
SRC=/Users/luzhoua/.gemini/antigravity/skills
DST=/Users/luzhoua/Mindhikers/.claude/skills

# 备份 stub
mv "$DST/Director" "$DST/Director.stub-bk-20260427"

# 复制 4 个轻量 skill
cp -R "$SRC/Director" "$DST/Director"
cp -R "$SRC/svg-architect" "$DST/svg-architect"
cp -R "$SRC/remotion-best-practices" "$DST/remotion-best-practices"
cp -R "$SRC/remotion-visual-qa" "$DST/remotion-visual-qa"

# rsync RemotionStudio 排除测试产物
rsync -a \
  --exclude='out/' --exclude='renders/' --exclude='payloads/' \
  --exclude='test_file.mp4' --exclude='test_payload.json' \
  --exclude='render_*.sh' --exclude='render_verification.sh' \
  --exclude='run_verification.js' --exclude='.DS_Store' \
  "$SRC/RemotionStudio/" "$DST/RemotionStudio/"
```

### 2. .env 切换

```diff
- SKILLS_BASE=/Users/luzhoua/.gemini/antigravity/skills
- REMOTION_STUDIO_DIR=/Users/luzhoua/.gemini/antigravity/skills/RemotionStudio
+ SKILLS_BASE=/Users/luzhoua/Mindhikers/.claude/skills
+ REMOTION_STUDIO_DIR=/Users/luzhoua/Mindhikers/.claude/skills/RemotionStudio
```

### 3. server/skill-loader.ts 清理

- 删除 `SKILL_SEARCH_PATHS` 顶层常量（曾把 `~/.gemini/antigravity/skills` 写死为第一优先级）
- 改用 `getSkillSearchPaths()` 函数：每次调用时 `process.env.SKILLS_BASE`（避免 ESM 顶层缓存早于 dotenv.config()，参考 rules #7）
- 全文 7 处引用替换

### 4. 启动验证

```
[0] 🔄 Starting Skill Sync...
[0] ✅ RemotionStudio reachable: /Users/luzhoua/Mindhikers/.claude/skills/RemotionStudio
[0] ✅ svg-architect reachable: /Users/luzhoua/Mindhikers/.claude/skills/svg-architect
[0] ✅ Skill Sync Complete. Synced: 6/7
[0] 🔒 Server running on http://127.0.0.1:3005
```

铁证：日志显示新 SSOT 路径已生效，`.bin/remotion` 软链接也正确传递（rsync `-a` 默认保留符号链接）。

## 已知 Caveat

### Skill Sync 6/7（缺 ThumbnailMaster）

`Mindhikers/.claude/skills/` 下无 ThumbnailMaster 目录（用户 Q1=C 范围未含）。

- skill-sync.ts 期望 5 个专家全在
- 当前 4 个专家（Director / Music / Shorts / Marketing）+ RemotionStudio + svg-architect = 6/7
- 影响：切换到"缩略图大师"专家时 ChatPanel 退化通用助手；导演工作流不受影响
- 后续如需补：从 `~/.gemini/antigravity/skills/ThumbnailMaster/` cp 一份过去即可

### 3 个 server 文件的 .gemini 硬编码 fallback 未清

- `server/svg-architect.ts:19`
- `server/director.ts:687`
- `server/skill-sync.ts:91, 106`

都是候选数组的第三/最后优先级。`SKILLS_BASE` 在前面且 Mindhikers 路径完整命中，旧路径永远走不到。功能不影响，但违反 rules.md #8（路径解析必须统一走 helper）。**留给下一窗口顺手清**。

## 本窗口刻意没做（让位给 SSOT 迁移）

老卢之前点了头要做的 3 件 UI 优化：

1. 底部 `DeliveryStatusBar` 内容右对齐 + 移除 LLM/Remotion/版本号
2. `RuntimePanel` 删写死的 SkillInfoCard，改为订阅 `skill-sync-status` 显示真实列表
3. 右栏 ChatPanel 紧凑化（`.shell-drawer__content` padding 收成 0）

**全部跳过**了，留给下一窗口接着做。

## 服务现状

- 后端 `http://127.0.0.1:3005` LISTEN（PID 见 `/tmp/director-dev.log`）
- 前端 `http://localhost:5178/` LISTEN
- 启动日志：`/tmp/director-dev.log`
