# 2026-04-30 Dev Log

## SaaS Shell Staging Apply + Railway Snapshot 修复

时间：2026-04-30 13:20 CST

分支：`MHSDC-GC-SAAS-staging`

### 背景

老卢确认继续主轴工作：将 SSE 壳层迁移成果接入 SaaS staging，并确认 Roundtable production/staging 入口应露出。

### 本轮完成

- 从 `MHSDC-GC-SAAS-staging` 新建接收分支 `codex/saas-shell-staging-apply`
- 依次 cherry-pick：
  - `b4c8ba2` → `e17f5d2`
  - `a316677` → `e610631`
- fast-forward 合入 `MHSDC-GC-SAAS-staging`
- 推送 `origin/MHSDC-GC-SAAS-staging`
- 本地通过：
  - `npm run typecheck:saas`
  - `npm run build`

### Railway 部署排障

初次部署失败在 Build 前：

```text
Initialization > Snapshot code
Failed to create code snapshot. Please review your last commit, or try again.
```

定位：

- 不是 TypeScript / Vite 构建失败
- Railway 未能创建 code snapshot
- 仓库内存在本地 agent/worktree 元数据与依赖备份目录
- `.railwayignore` 未明确排除 `.claude/`、`.vibedir/`、`.playwright-mcp/`、`.codepilot-uploads/` 等目录

修复：

- 收紧 `.railwayignore`
- 排除本地 agent 元数据、runtime 产物、testing、坏依赖备份、本地 smoke 脚本等

### 部署结果

- Railway service：`golden-crucible-saas`
- Environment：`staging`
- 成功 deployment：`5f18972a-be9e-478b-8ac7-36f6cc8681d7`
- Build 证据：
  - `✓ 2084 modules transformed`
  - `dist/assets/index-DmwApuSI.js`
- `/health` 返回 `status: ok`

### 线上验收

agent-browser 已验证：

- 未登录 `/`：认证页正常
- 登录后新 Shell 可见
- 左侧模块显示 `炼制` 与 `圆桌`
- `/m/roundtable` 可进入，显示输入命题与开始圆桌按钮
- `/llm-config` 正常
- 无 Hooks error
- 无 ShellErrorBoundary
- Network 无非预期 localhost 直连

### 注意事项

- Roundtable 后端 API 仍未完整接入；本轮完成的是壳层入口与页面可见性
- 早前脏现场仍保存在 stash：
  - `stash@{0}: codex pre shell staging apply cleanup 2026-04-29`
- 该 stash 后续应单独整理，不与壳层部署修复混提交
