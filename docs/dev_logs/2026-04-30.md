# 2026-04-30 Dev Log

## SaaS Shell Staging Apply + Railway Snapshot 修复

时间：2026-04-30 13:20 CST

分支：`MHSDC-GC-SAAS-staging`

### 背景

老卢确认继续主轴工作：将 SSE 壳层迁移成果接入 SaaS staging，并确认 Roundtable production/staging 入口应露出。

### 本轮完成

- 从 `MHSDC-GC-SAAS-staging` 新建接收分支 `codex/saas-shell-staging-apply`
- 依次 cherry-pick：
  - `b4c8ba2` → `e17f5d2`
  - `a316677` → `e610631`
- fast-forward 合入 `MHSDC-GC-SAAS-staging`
- 推送 `origin/MHSDC-GC-SAAS-staging`
- 本地通过：
  - `npm run typecheck:saas`
  - `npm run build`

### Railway 部署排障

初次部署失败在 Build 前：

```text
Initialization > Snapshot code
Failed to create code snapshot. Please review your last commit, or try again.
```

定位：

- 不是 TypeScript / Vite 构建失败
- Railway 未能创建 code snapshot
- 仓库内存在本地 agent/worktree 元数据与依赖备份目录
- `.railwayignore` 未明确排除 `.claude/`、`.vibedir/`、`.playwright-mcp/`、`.codepilot-uploads/` 等目录

修复：

- 收紧 `.railwayignore`
- 排除本地 agent 元数据、runtime 产物、testing、坏依赖备份、本地 smoke 脚本等

### 部署结果

- Railway service：`golden-crucible-saas`
- Environment：`staging`
- 成功 deployment：`5f18972a-be9e-478b-8ac7-36f6cc8681d7`
- Build 证据：
  - `✓ 2084 modules transformed`
  - `dist/assets/index-DmwApuSI.js`
- `/health` 返回 `status: ok`

### 线上验收

agent-browser 已验证：

- 未登录 `/`：认证页正常
- 登录后新 Shell 可见
- 左侧模块显示 `炼制` 与 `圆桌`
- `/m/roundtable` 可进入，显示输入命题与开始圆桌按钮
- `/llm-config` 正常
- 无 Hooks error
- 无 ShellErrorBoundary
- Network 无非预期 localhost 直连

### 注意事项

- Roundtable 后端 API 仍未完整接入；本轮完成的是壳层入口与页面可见性
- 早前脏现场仍保存在 stash：
  - `stash@{0}: codex pre shell staging apply cleanup 2026-04-29`
- 该 stash 后续应单独整理，不与壳层部署修复混提交

---

## Governance Closure Alignment

时间：2026-04-30 17:30 CST

按 SSE 侧 `docs/plans/2026-04-30-001-gc-sse-saas-governance-closure-plan.md` 对齐 SaaS 治理记录。

### 当前统一口径

- SaaS staging 已接收 shell migration。
- Railway snapshot 修复已完成。
- Staging smoke 已完成，但带已知后续项：
  - SkillSync lower-right indicator 未完成。
  - SkillSync SSOT source popover 未完成。
  - Roundtable backend API completion 未完成。
  - Left module glyph alignment 未完成。
- 下一功能切片从 SSE 开始，SaaS staging 只接收验证后 cherry-pick。

---

## Shell / Status Polish Apply

时间：2026-04-30 18:20 CST

分支：`codex/saas-shell-status-polish-apply`

来源 SSE commit：

```text
03fbb9d refs MIN-136 fix: polish shell module and SkillSync status
```

### 本轮接收内容

- `ModuleTab` glyph 固定为 24px 居中盒，`坩 / 辩` 与 `炼制 / 圆桌` 对齐更稳定。
- 新增 `src/shell/SkillSyncStatus.tsx` 与 CSS module。
- `ShellLayout` 右下角展示 SkillSync 状态按钮。
- 点击按钮展示：
  - runtime status
  - SSOT source root
  - target root
  - synced skills
- `server/skill-sync.ts` 的状态 payload 补齐：
  - `sourceRoot`
  - `targetRoot`
  - `expected`

### 待完成验证

- `npm run typecheck:saas`
- `npm run build`
- agent-browser `/`、`/m/crucible`、`/m/roundtable`、`/llm-config`
- Network 无非预期 localhost 直连
