# MarketingMaster 测试队列

当前目录是 `MarketingMaster` 模块测试协作区。

读取顺序：

1. `testing/README.md`
2. `testing/OPENCODE_INIT.md`
3. `testing/marketing-master/README.md`

目录约定：

- `requests/`：测试请求
- `claims/`：认领记录
- `reports/`：测试报告
- `artifacts/`：截图与日志
- `status/`：状态板

环境 ready 后等待用户给出下一步测试计划，不自动执行 request。

